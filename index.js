/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';
import LineGraph from './lineGraph';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => LineGraph);
